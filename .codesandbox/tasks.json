{
  "setupTasks": [],

  "tasks": {
    "App": {
      "name": "running application",
      "command": "cd backend && yarn && cd ../frontend && yarn && yarn run dev",
      "runAtStart": true
    }
  }
}
